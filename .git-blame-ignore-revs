# Revisions to skip when running `git blame`.
#
# The 2026-08 restructure (docs/archive/REFACTOR_PLAN.md) moved almost every
# file into src/maceh/, workflows/, and tests/ without changing what the code
# does. Left in the blame output those commits attribute every line to the
# move rather than to the change that actually introduced it.
#
# Append the full 40-character SHA of each pure-move commit below, one per
# line, after you commit the restructure. GitHub reads this file automatically;
# locally, enable it once with:
#
#     git config blame.ignoreRevsFile .git-blame-ignore-revs
6f66918e0eac7a0651f23baaa55600e327c07c4d
