"""Training handoff for the MgO LR experiment.

`train_sr.ini` / `train_full.ini` are the paired production runs;
`make_trainval_view.py` builds the dataset directory they read and
`check_split_wiring.py` proves they reproduce the frozen `organize` splits.
See `README.md` in this directory.
"""
